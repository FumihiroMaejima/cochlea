[imagick]
; Avoid locale-related issues in some environments
; imagick.locale_fix=1

; Skip the runtime version check
; imagick.skip_version_check=1

; Limit resource usage (threads) if needed
; imagick.set_single_thread=1
; imagick.thread_limit=2

; Temporary directory for Imagick
; imagick.tmp_dir=/tmp
